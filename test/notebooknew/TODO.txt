# NotebookNode

- "TODO" invullen
- Notebook.ui_prefs
  - set attributes -> save -> get attributes
- multiple trash nodes
- trash parent != root
- repairing incorrect structures
- NotebookError
- NotebookVersionError
- basetreeview._add_model_column(): get = lambda ...
- integration test: opening a notebook with 1000 assets takes <= 0.5 seconds
- integration test: saving a notebook with 1000 assets takes <= 0.5 seconds
- _add_unbound_node_as_child() invoeren -- notebook en parent als constructorparameter verwijderen en _add_child_node() vervangen
- modification time when changing payload (depends on whether the changes come from the DAO or not)
- create root node after first sync
- create trash on first delete

x create_copy()
- receive_moving_node(node, after)

Notebook
	default_font   The default font for new nodes.
	icons          A set of icons that can be assigned to nodes.

ContentNode/FolderNode/TrashNode
	modifying order -> is dirty == True
	test that order actually determines the order (or does the ListView take care of that?)

ContentNode/FolderNode
	trash()

TrashNode
	empty()

NotebookTest
	test_node_changed_event_raised() (currently @unittest.skip)
